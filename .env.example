# n8n MCP Server Configuration
# Copy this file to .env and configure your n8n instance

# n8n API Configuration (Required)
N8N_API_URL=http://localhost:5678/api/v1
N8N_API_KEY=your_n8n_api_key_here

# Optional: Webhook authentication (if using webhook features)
N8N_WEBHOOK_USERNAME=webhook_user
N8N_WEBHOOK_PASSWORD=webhook_pass

# Debug mode (optional)
DEBUG=false

# ================================================================
# SETUP INSTRUCTIONS:
# ================================================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Start your n8n instance and get your API key:
#    - Open n8n in browser (usually http://localhost:5678)
#    - Go to Settings > API Keys
#    - Generate a new API key
#    - Copy the key and paste it as N8N_API_KEY above
#
# 3. Update N8N_API_URL if your n8n runs on different host/port
#
# 4. Save the file and restart the MCP server
#
# ================================================================
